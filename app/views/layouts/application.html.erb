<!DOCTYPE html>
<html>
  <head>
    <title>BikeReviewMap</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <!-- OGP 基本設定 -->
    <meta property="og:title" content="Bike Review Map" />
    <meta property="og:description" content="全国の自転車店レビューを地図で検索できるサービス" />
    <meta property="og:image" content="<%= image_url('ogp.png') %>" />
    <meta property="og:url" content="https://bike-review-map.fly.dev" />
    <meta property="og:type" content="website" />

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", type: "module" %>
  </head>

  <body class="bg-gray-50 text-gray-900">

    <!-- Header -->
    <header class="bg-gray-900 text-white py-4 px-6 flex justify-between items-center shadow-md">
      <%= link_to '🚲 Bike Review Map', root_path, class: "text-2xl font-bold hover:underline" %>
      <nav class="space-x-4 text-sm">
      <%= link_to '検索', search_shops_path, class: "hover:underline" %>
      <% if user_signed_in? %>
        <%= link_to "#{current_user.name} さん", user_path(current_user), class: "hover:underline text-white font-semibold" %>
        <%= link_to 'ログアウト', destroy_user_session_path, method: :delete, data: { turbo: false, confirm: 'ログアウトしますか？' }, class: "hover:underline" %>
      <% else %>
        <%= link_to '新規登録', new_user_registration_path, class: "hover:underline" %>
        <%= link_to 'ログイン', new_user_session_path, class: "hover:underline" %>
      <% end %>
      </nav>
    </header>

    <!-- フラッシュメッセージ -->
    <div class="container mx-auto mt-4 px-4">
      <% if notice %>
        <div class="bg-green-100 border border-green-400 text-green-700 px-4 py-2 rounded mb-4">
          <%= notice %>
        </div>
      <% end %>

      <% if alert %>
        <div class="bg-red-100 border border-red-400 text-red-700 px-4 py-2 rounded mb-4">
          <%= alert %>
        </div>
      <% end %>
    </div>

    <!-- 各ページの内容 -->
    <%= yield %>
  </body>
</html>
